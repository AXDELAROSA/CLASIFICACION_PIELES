	USE DATA_02
	SELECT CONVERT(DATE, (GETDATE()-(DAY(GETDATE())-1))) 'Primer Dia del Mes'
	SELECT  CONVERT(VARCHAR(10),GETDATE(), 105)  'Fecha(DD-MM-YYYY)'

	
	--DECLARE @VP_ID INT = 0, @VP_CONSECUTIVO INT = 1, @VP_HIDE VARCHAR(10)= '';
	--DECLARE CU_PIELES_CONS_INC CURSOR 
	--FOR SELECT ID, HIDE
	--	FROM HIDESLIN_SQL (NOLOCK)
	--	WHERE FILENO = '220311135023T1000001'
	--	ORDER BY ID ASC
	
	--OPEN CU_PIELES_CONS_INC
	--FETCH NEXT FROM CU_PIELES_CONS_INC INTO @VP_ID, @VP_HIDE
	
	--WHILE @@FETCH_STATUS = 0
	--	BEGIN	
	--		--SELECT RIGHT('0000' + CONVERT(VARCHAR(5), @VP_CONSECUTIVO), 4) AS NEW, @VP_HIDE AS OLD
			
	--		--UPDATE HIDESLIN_SQL
	--		-- SET HIDE = RIGHT('0000' + CONVERT(VARCHAR(5), @VP_CONSECUTIVO), 4)
	--		--WHERE ID = @VP_ID AND FILENO = '220311135023T1000001'

	--		SET @VP_CONSECUTIVO = @VP_CONSECUTIVO + 1
	--		FETCH NEXT FROM CU_PIELES_CONS_INC INTO @VP_ID, @VP_HIDE
	--	END
	--CLOSE CU_PIELES_CONS_INC
	--DEALLOCATE CU_PIELES_CONS_INC

	-- ////////////////////////////////////////////////////////////// 
	USE DATA_02Pruebas

	SELECT * FROM HIDESHDR_SQL   
	 inner join HIDESLIN_SQL on  HIDESLIN_SQL.FILENO = HIDESHDR_SQL.FILENO
	WHERE	LTRIM(RTRIM(PLOT)) IN ('104833')
	AND CONVERT(INT, LTRIM(RTRIM(HIDE)))  IN ( 79 )
	----AND
	 --PCOLOR = 'FCPRHL1'
	 --AND SQF < 1
	 ORDER BY HIDESLIN_SQL.HIDE ASC
			
	SELECT SUM(sqf) AS TOTAL_SQF FROM [TICKET_A_IMPORTAR] WHERE lote = '69063'   AND L_BORRADO = 0  --AND FILE_NO in (  '211111094233T1000001')
		
		--USE DATA_02PRUEBAS
	SELECT * FROM [TICKET_A_IMPORTAR] WHERE lote LIKE '%69063%' --AND L_BORRADO = 0 --AND FILE_NO in (  '211111094233T1000001') 
	order by [K_TICKET_A_IMPORTAR] DESC

	select * from PIEL_A_TRANSFERIR
	select * from INSPECCION_LOTE_PIEL_RESULTADO_LOG where K_IMPORTACION_LOTE_PIEL = 7962

	SELECT TOP 30 * 
	FROM IncInsp_sql 
	WHERE ID = 8184 
	ORDER BY ID DESC

	SELECT TOP 100 * 
	FROM HIDESHDR_SQL 
	WHERE PLOT IN ('31151') AND STATUS = 'MIT' --AND  FILENO IN  ( '20211228125704' )
	ORDER BY ID DESC	

	SELECT * FROM RP_SC WHERE LOT = '35951'
	AND CONVERT(INT, LTRIM(RTRIM(HIDE)))  IN ( 
	SELECT HIDE 
	FROM HIDESLIN_SQL
	WHERE FILENO IN ( '220330092131T1000001' )
	)

	SELECT * 
	FROM HIDESLIN_SQL
	WHERE FILENO IN ( '220330092131T1000001' )

	SELECT * 
	FROM HIDESHDR_SQL   
	 inner join HIDESLIN_SQL on  HIDESLIN_SQL.FILENO = HIDESHDR_SQL.FILENO
	WHERE	LTRIM(RTRIM(PLOT)) IN ('401201')
	AND CONVERT(INT, LTRIM(RTRIM(HIDE))) IN (209)
	ORDER BY hide
	 
	SELECT TOP 1000
			D_TIPO_PIEL_LOG, 
			( CASE WHEN usuario IS NULL THEN (	SELECT CONCAT(EP_NOMBRE, ' ', EP_APELLIDO_PATERNO) 
												FROM HOWE.dbo.VISTA_GAFETES (NOLOCK)
												WHERE EN_NUM_EMP = K_USUARIO_ALTA
											)
				ELSE usuario END) AS RESPONSABLE, 
			PIEL_LOG.*
	FROM PIEL_LOG (NOLOCK)
	LEFT JOIN users_pearl (NOLOCK) ON PIEL_LOG.K_USUARIO_ALTA = users_pearl.codigo
	INNER JOIN TIPO_PIEL_LOG (NOLOCK) ON PIEL_LOG.K_TIPO_PIEL_LOG = TIPO_PIEL_LOG.K_TIPO_PIEL_LOG
	WHERE ( FOLIO_ORIGEN = 3905182 OR  FOLIO_DESTINO = 3905182 ) 
	--(ORDEN_ORIGEN = '16773' OR  ORDEN_DESTIDO = '16773' )
	--(LOCACION_ORIGEN = 'GT3' OR  LOCACION_DESTINO = 'GT3' ) AND
	--LOTE IN ( 106221     ) 
	--AND  PIEL   IN  ( 29 )
	--AND PIEL_LOG.K_TIPO_PIEL_LOG  IN (1)
	ORDER BY  K_PIEL_LOG desc
	
	select * from TIPO_PIEL_LOG

	 SELECT * 
	 FROM cccuthst_sql
	 WHERE -- jobno IN ('59476') order by colkey desc
	----AND 
	LTRIM(RTRIM(LOTNO)) = RIGHT('000000' + CONVERT(VARCHAR(20), 106221     ), 6) -- 105371    
	AND	CONVERT(INT, hideno)  IN ( 29 )
	--ORDER BY hideno

	--UPDATE cccuthst_sql
	--	SET hideno = '0000'
	-- WHERE --jobno IN ('45874') order by colkey desc
	----AND 
	--LTRIM(RTRIM(LOTNO)) = RIGHT('000000' + CONVERT(VARCHAR(20), 106221     ), 6) -- 105371    
	--AND	CONVERT(INT, hideno)  IN ( 29 )

	
	SELECT * FROM  RP_SC WHERE --LOT = '34281' AND CONVERT(INT, hide)  IN ( 624, 626 )
	TAGNO IN (3905182) 
	ORDER BY HIDE
	
	SELECT * FROM RP_SC_PRT

	 SELECT * FROM COLORES_CONTROLADOS WHERE COLOR IN ('FNPJTX7')
	 
	 SELECT * FROM pearl_log WHERE jobno  IN ('57893')  
	
	SELECT * FROM   RP_Folios  WHERE TAG IN ( 3905182)

	 SELECT * FROM ccjobhdr_sql WHERE jobno IN ('59335', '48665') 
	 SELECT * FROM HEADER_RMA WHERE JOBNO = '59335'
	 
	--	SELECT * FROM RP_SC_FOLIO_STATUS_XX WHERE tagno IN ( 3823391) ORDER BY HIDE

	SELECT * FROM	IMLSMST_SQL	WHERE	--LTRIM(RTRIM(item_no)) = 'FMCKSTT        '  and 
	LTRIM(RTRIM(loc)) = 'MHI' --and 
	AND
	LTRIM(RTRIM(SER_LOT_NO)) IN ('031121')  
	--AND qty_on_hand > 0
	ORDER BY item_no

	SELECT * FROM	IMLSMST_SQL	WHERE	--LTRIM(RTRIM(item_no)) = 'FMCKSTT        '  and 
	LTRIM(RTRIM(loc)) = 'T05' --and 
	AND
	LTRIM(RTRIM(SER_LOT_NO)) IN ('031121')  
	--AND qty_on_hand > 0
	ORDER BY item_no
	
	--update IMLSMST_SQL	
	--	set qty_on_hand = qty_on_hand - 289.2000
	--WHERE	LTRIM(RTRIM(SER_LOT_NO)) IN ('068151') and loc = 'GT2' and item_no = 'FNRUDX9'

	--SELECT TOP 10 * FROM	IMINVLOC_SQL	
	--WHERE	loc = 'T05' and item_no = 'FCMKSTT'

	-- 20401.4000 - 53 = 20454.4

	--update IMINVLOC_SQL	
	--	set qty_on_hand = qty_on_hand + 0.0017
	-- WHERE	loc = 'T21' and item_no = 'FCNPDX9'

	USE DATA_02 --PRUEBAS
	SELECT TOP 100 * FROM   IMINVTRX_SQL 
	WHERE ITEM_NO = 'FCPRDX9' AND LOC = 'T65' AND trx_dt = '20220119'
	-- and doc_ord_no = '30284' 
	 --AND TRX_DT = 20210824
	ORDER BY A4GLIdentity DESC

	SELECT * FROM IMLSTRX_SQL WHERE ord_no IN (5631797   )
	 
	SELECT TOP 100 * FROM IMINVTRX_SQL WHERE ord_no IN ( 5631797  )


	-- //////////AGREGAR CORTE MANUAL////////////////////////////////////////////////////
	--DECLARE @VP_FECHA	INT = FORMAT(CAST(GETDATE() AS DATE), N'yyyyMMdd');
	--DECLARE @VP_HORA	INT = FORMAT(CAST(GETDATE() AS TIME(0)), N'hhmmss');
	--DECLARE @VP_LOTE_CON_ESPACIO VARCHAR(50) = CONCAT('         ', RIGHT('000000' + '68962', 6) )
	--DECLARE @VP_TURNO	VARCHAR(5) = '0'
	
	--EXECUTE [dbo].[PG_IN_CCCUTHST_SQL]		0, 0,
	--										-- ===========================
	--										'00000', '0000', '1', @VP_FECHA,
	--										-- ===========================
	--										@VP_HORA, @VP_FECHA, 0, 'FMCKDX9', 
	--										-- ===========================
	--										@VP_LOTE_CON_ESPACIO, '0287','20.00',
	--										-- ===========================
	--										'00000', '', '00000', '00000',
	--										'00000', '00000', '00000', '00000', '00000',
	--										-- ===========================
	--										0, 0, 0, 0,
	--										-- ===========================
	--										1, 0, 'P', '', '', 'FRANCISCOE', 'IT-10'

	
-- //////////////////////////////////////////////////////////////
-- //////////////////////////////////////////////////////////////
